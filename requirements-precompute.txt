# PRECOMPUTE lane: the engines the bake actually runs (spinoct, numpy, scipy), pinned in
# data-pipeline/requirements.txt and documented in docs/frameworks/spinoct/.
-r data-pipeline/requirements.txt
