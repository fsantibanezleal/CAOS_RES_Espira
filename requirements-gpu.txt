# GPU lane: DORMANT. The bake runs on CPU today (numpy solvers, parallel over cases). A batched GPU image
# solver (torch) is planned for the full case matrix (programme backlog BL-009); when it lands, its pinned
# CUDA packages and driver matrix go here.
-r requirements-precompute.txt
